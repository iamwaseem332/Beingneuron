{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Gold Standard Entity Annotations",
  "description": "Ground truth entity annotations for benchmark corpus papers",
  "type": "object",
  "properties": {
    "annotations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "paperId": {
            "type": "string",
            "description": "Must match paperId in corpus metadata.json"
          },
          "entities": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "entityId": {
                  "type": "string",
                  "description": "Unique identifier within this paper (e.g., 'e1', 'e2')"
                },
                "text": {
                  "type": "string",
                  "description": "Exact text span as it appears in the paper"
                },
                "normalizedForm": {
                  "type": "string",
                  "description": "Canonical/normalized form of the entity (e.g., 'Transformer' for 'transformer model')"
                },
                "entityType": {
                  "type": "string",
                  "enum": ["concept", "method", "dataset", "claim", "author", "metric", "task"],
                  "description": "Category of the entity"
                },
                "page": {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Page number where entity appears (1-indexed)"
                },
                "startOffset": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Character offset from start of page content"
                },
                "endOffset": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Character offset to end of entity text"
                }
              },
              "required": ["entityId", "text", "normalizedForm", "entityType", "page", "startOffset", "endOffset"],
              "additionalProperties": false
            }
          }
        },
        "required": ["paperId", "entities"],
        "additionalProperties": false
      }
    }
  },
  "required": ["annotations"],
  "additionalProperties": false
}
